<p>
	<%= answer.content %>
	  <p>by <b><%= answer.user.name %></b></p>
	  <% if can? :destroy, answer %>
	   <%= link_to "Delete", question_answer_path(answer.question,answer), method: :delete %>
	  <% end %> 
	  
	   
</p>